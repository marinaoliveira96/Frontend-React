import UseRef from '../hooks/UseRef';
import UseReducer from '../hooks/UseReducer';
const Home = () => {
  return (
    <div>
      <h1>React Hooks</h1>

      <UseReducer />
    </div>
  );
};

export default Home;
