import UseRef from '../hooks/UseRef';
import UseCallback from '../hooks/UseCallback';

const Home = () => {
  return (
    <div>
      <h1>React Hooks</h1>

      <UseCallback />
    </div>
  );
};

export default Home;
